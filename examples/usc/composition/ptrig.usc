-- -*- mode: lua; -*-

return bd.system {
   imports = { "stdtypes", "ptrig" },

   blocks = {
      { name="ptrig1", type="ubx/ptrig" },
   },

   configurations = {
      {
	 name="ptrig1",
	 config = {
	    period = {sec=1, usec=0 },
	    chain0={ { b="#trig" } }
	 }
      },
   }
}
