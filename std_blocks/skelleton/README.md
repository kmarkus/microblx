# skelleton

Annotated template for writing new microblx c-blocks. Not a functional block.

Contains a complete, commented example with:
- a custom type (`struct foo_type`) declared and registered
- static configs and ports
- all lifecycle hooks (`init`, `start`, `step`, `stop`, `cleanup`)
- proper error handling and port caching patterns

Copy and rename as a starting point for a new block.
